encoder:
  in_dim: 4
  hidden_dim: 32 
  num_layers: 3
  num_bases: -1

lig_encoder:
  in_dim: 166
  hidden_dim: 32
  num_layers: 3

decoder:
  in_dim: 64 
  out_dim: 1 
  hidden_dim: 32
  num_layers: 3
  activation: sigmoid 

pool: 'att'
dropout: 0.2
batch_norm: true
use_pretrained: false
pretrained_path: pretrained/pretrained_model_graphlet.pth
