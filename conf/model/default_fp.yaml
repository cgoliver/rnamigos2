encoder:
  in_dim: 4
  hidden_dim: 32 
  num_layers: 3

lig_encoder:
  in_dim: 166
  hidden_dim: 32
  num_layers: 3

decoder:
  in_dim: 32
  out_dim: 166
  hidden_dim: 64
  num_layers: 3
  activation: null

pool: 'att'
use_pretrained: false
pretrained_path: pretrained/pretrained_model_graphlet.pth
